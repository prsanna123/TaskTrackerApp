
{
    "version": 2,
    "builds": [
      { "src": "./index.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/auth/(.*)", "dest": "/api/auth/$1" },
      { "src": "/api/tasks/(.*)", "dest": "/api/tasks/$1" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  